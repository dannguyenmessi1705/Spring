server:
  port: 8071
spring:
  application:
    name: "configserver"
  profiles:
    active: git
    #active: native
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/danprohy/configserver-microservices.git"
          default-label: "master"
          timeout: 5
          clone-on-start: true
          force-pull: true
        #native:
          #search-locations: "classpath:/config"
encrypt:
  key: "ff1e5a8caca2b09be73ac311a464be7d"
management:
  endpoints:
    web:
      exposure:
        include: "*"
