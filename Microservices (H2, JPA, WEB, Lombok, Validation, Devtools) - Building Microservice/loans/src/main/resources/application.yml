server:
 port: 8090
spring:
 datasource:
  url: jdbc:h2:mem:testdb
  driver-class-name: org.h2.Driver
  username: sa
  password: ''
 h2:
  console:
   enabled: true
 jpa:
  database-platform: org.hibernate.dialect.H2Dialect
  hibernate:
   ddl-auto: update
  show-sql: true
 profiles:
  active: ''
springdoc:
 api-docs:
  path: /api-docs
 swagger-ui:
  path: /swagger-ui

build:
 version: "1.0"

loans:
 message: "Welcome to Bank loans related local APIs"
 contactDetails:
  name: "Nguyen Di Dan - Developer"
  email: "didan@dev.com"
 onCallSupport:
  - (843) 456-6789
  - (846) 989-2223